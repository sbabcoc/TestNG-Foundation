<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.nordstrom.test-automation.tools</groupId>
  <artifactId>testng-foundation</artifactId>
  <version>99999-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>TestNG Foundation</name>
  <description>This is the foundation framework for TestNG automation via Selenium WebDriver and RESTeasy</description>
  <url>https://git.nordstrom.net/projects/MFATT/repos/testng-foundation/browse</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <automation-framework-core.version>0.3.0.0</automation-framework-core.version>
    <compiler-plugin.version>3.6.0</compiler-plugin.version>
    <source-plugin.version>3.0.1</source-plugin.version>
    <javadoc-plugin.version>2.10.4</javadoc-plugin.version>
    <deploy-plugin.version>2.8.2</deploy-plugin.version>
  </properties>

  <distributionManagement>
    <repository>
      <id>releases</id>
      <url>https://mvnrepo.nordstrom.net/nexus/content/repositories/releases</url>
    </repository>
  </distributionManagement>
  
  <dependencies>
    <dependency>
      <groupId>com.nordstrom.test-automation.tools</groupId>
      <artifactId>automation-framework-core</artifactId>
      <version>${automation-framework-core.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler-plugin.version}</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>${source-plugin.version}</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>${javadoc-plugin.version}</version>
        <executions>
          <execution>
            <id>attach-javadocs</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${deploy-plugin.version}</version>
        <executions>
          <execution>
            <id>deploy-file</id>
            <phase>deploy</phase>
            <goals>
              <goal>deploy-file</goal>
            </goals>
            <configuration>
              <file>${project.build.directory}${file.separator}${project.build.finalName}.jar</file>
              <repositoryId>releases</repositoryId>
              <url>https://mvnrepo.nordstrom.net/nexus/content/repositories/releases</url>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
